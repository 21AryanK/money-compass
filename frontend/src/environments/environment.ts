// Production build. API_BASE_URL from infra/.env should be substituted here at
// build time, or replaced with the deployed origin before `ng build`.
export const environment = {
  production: true,
  apiBaseUrl: '/api',
};
