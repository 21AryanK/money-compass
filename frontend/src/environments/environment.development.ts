// Local development. Matches the backend's default port and the origin listed
// in CORS_ALLOWED_ORIGINS.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:8080/api',
};
